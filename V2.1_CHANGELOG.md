# ComfyUI Remote Panel V2.1.0

## V2.1：输入常驻、批量顺序生成、精简主页与个性化

### 1. 工作流切换更顺手
- 保留首页横向工作流快捷按钮。
- 新增“工作流列表”，工作流很多时可直接从列表切换。
- 收藏置顶、重命名、删除等 V2.0 功能保持不变。

### 2. 输入图片按工作流持久保存
- V2.0 提交任务后会清空输入图；V2.1 不再自动清空。
- 每个工作流分别保存自己的输入图片 URI 与“替换此输入”开关状态。
- 切换工作流、退出 App、重新打开后会恢复该工作流上次选择的输入图，并重新上传到当前 ComfyUI。
- 增加“清空本工作流已记住的输入图”按钮。

### 3. 多选图片按顺序批量生成
- 每个图片输入新增“选择单张”和“批量选择”。
- 批量选择多张图片后会二次确认，再使用独立单线程逐张上传。
- App 按 Android 文件选择器返回的 ClipData 顺序逐张创建任务，避免并发上传造成顺序错乱。
- 多输入工作流中，批量操作只替换所点击的那个输入，其余启用输入沿用当前图片。
- 单张生成与批量生成都继续复用 V2.0 的后台提交队列和 ComfyUI 队列。

> 说明：App 会严格保留系统文件选择器返回的顺序；不同 Android 文件选择器是否把这个顺序定义为“点击选择顺序”由对应系统/文件管理器决定。

### 4. 首页默认精简
- 默认直接显示：工作流、输入图片、最近输出、生成/队列/图库。
- ComfyUI 连接、提示词/开关、输出节点默认折叠到“高级设置”。
- 不删除任何高级功能，只减少日常使用时的滚动距离。

### 5. 外观与个性化
- 继续支持跟随系统 / 浅色 / 深色模式。
- 可从相册/文件选择任意图片作为主界面背景。
- 可选择任意图片作为启动页；无法读取时自动恢复默认启动页。
- 新增独立启动页 Activity。
- 桌面 App 图标提供 3 个 APK 内置样式即时切换：默认霓虹节点沙发 / 极简节点 / 蓝色节点。

### 关于“任意相册图片直接改桌面 App 图标”
Android 的 Launcher 图标必须来自 APK 已打包资源；已安装 App 不能把任意外部图片直接变成系统桌面图标。V2.1 因此采用 Activity Alias 提供 3 个预置图标即时切换，没有伪装成支持系统做不到的任意图片动态图标。主界面背景和启动页不受这个限制，可使用任意图片。

## 版本
- versionCode: 11
- versionName: 2.1.0
