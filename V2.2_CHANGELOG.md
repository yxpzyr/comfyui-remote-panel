# ComfyUI Remote Panel V2.2.0

## V2.2：手机端蒙版遮罩编辑器

V2.2 在 V2.1 的输入常驻、批量顺序生成和个性化基础上，加入 **ComfyUI MASK 输入工作流的手机端蒙版绘制能力**。

### 1. 自动识别支持蒙版的输入图片节点

- 对核心 `LoadImage`、`LoadImageMask(alpha)`、`LoadImageOutput` 等常见节点做离线识别。
- App 连接 ComfyUI 后还会读取 `/object_info`；如果自定义图片加载节点存在 `MASK` 输出，也会动态开启蒙版入口。
- 普通不支持 MASK 的图片输入节点不会显示蒙版按钮，避免主界面变乱。
- `LoadImageMask` 如果当前选择的是 red/green/blue 通道，不显示 Alpha 蒙版编辑器，避免生成无效遮罩。

### 2. 新增手机端蒙版编辑器

支持：

- 手指涂抹蒙版
- 橡皮擦
- 画笔大小调节
- 撤销 / 重做（最多保留 15 步）
- 反转蒙版
- 一键清空
- 已有蒙版再次打开继续编辑
- 红色半透明区域实时显示最终 MASK 范围

### 3. 与 ComfyUI 原生 LoadImage MASK 机制兼容

ComfyUI 核心 `LoadImage` 的 MASK 定义为 `1 - alpha`。V2.2 保存蒙版时：

- 原图 RGB 像素保持不变；
- 未涂抹区域 Alpha = 255；
- 涂抹区域 Alpha = 0；
- 上传为 PNG；
- 因此 LoadImage 的 IMAGE 输出仍是原图，而 MASK 输出会得到用户绘制的遮罩。

不需要向工作流额外插入节点，也不会修改工作流拓扑。

### 4. 蒙版按工作流 / 输入节点持久化

- 每个工作流的每个图片输入分别记住自己的蒙版文件。
- 切换工作流后再切回来仍可继续编辑。
- App 重启后仍能恢复。
- 更换输入图片时会自动清除旧蒙版，防止尺寸/位置错位。
- “清空本工作流已记住的输入图”会同时清理该工作流保存的蒙版。

### 5. 上传与任务逻辑

- 有蒙版：上传 App 生成的 RGBA PNG。
- 无蒙版：继续上传用户原图，行为与 V2.1 相同。
- 清除蒙版后会重新上传未带蒙版的原图。
- 批量选择不会把当前单张图片的蒙版复用到其它图片，避免蒙版错位；批量任务仍按 V2.1 顺序上传与入队。

### 版本

- versionCode: 12
- versionName: 2.2.0
