# ComfyUI Remote Panel V2.0.0

## 🎉 V2.0：多输出工作流 + 正式 App 图标

### 多输出节点选择
- 导入工作流时读取 ComfyUI `/object_info`，使用节点定义中的 `output_node=true` 识别真实输出节点。
- 无法连接 `/object_info` 时，使用 SaveImage / PreviewImage / SaveVideo 等常见输出类名做离线回退识别。
- 主界面新增“输出节点”卡片，每个输出点都有独立开关。
- 至少必须保留一个输出节点，避免提交无输出 Prompt。
- 每个工作流独立保存自己的输出节点选择，切换工作流互不影响。
- 点击“重新检测输出节点”可按当前电脑端 ComfyUI 节点定义刷新。
- 提交任务时从 API Prompt 中移除未选中的 output sink，因此不需要的输出分支不会作为最终输出执行。
- history 图片同步会按本任务选择的输出节点过滤，避免多个输出点混在一起。
- 队列卡片会显示本任务选择了哪些输出节点。

### V2.0 正式图标
- 不再使用 Android 默认图标。
- 新图标以“Comfy = 舒适沙发 + UI = 节点连线”为核心概念：霓虹节点网络组成沙发，上方图片节点与星光表示 AI 图像生成。
- 提供多密度 launcher PNG，并加入 Android 8+ adaptive icon。

### 保留 V1.9 功能
- 跟随系统 / 浅色 / 深色模式。
- 多工作流常驻。
- 输入图片预上传、连续快速排队。
- WebSocket 进度 + history/queue 回退。
- 后台/锁屏恢复最近输出。
- 100 张实时图库、排序、多选批量保存。
- 队列缩略图、进度、耗时、原图预览、长按保存。

## 版本
- versionCode: 10
- versionName: 2.0.0
