# ComfyUI Remote Panel V1.9.0

## 目标
V1.9 针对 V1.8 实际夜间使用反馈，重点解决：夜间模式、图库不追最新、连续排队体验不够像 ComfyUI、切后台后最近输出不刷新、队列缺少缩略图/进度/耗时/原图查看。

## 主要变化

### 1. 夜间模式
- 新增「跟随系统 / 浅色 / 深色」三档外观设置。
- 主界面、工作流卡片、队列、图库、图片预览弹窗统一使用动态主题色。
- 深色模式同步处理状态栏、导航栏、卡片、文字、占位图和选中态。

### 2. 更接近网页版 ComfyUI 的输入与连续提交
- 工作流仍然只需导入一次，后续直接使用已经转换并保存的 API Prompt，不再每次生成重新请求 `/object_info` 做工作流转换。
- 选择输入图片后立即在后台上传到 ComfyUI；上传完成后显示「已上传，可开始生成」。
- 「加入队列」按钮改为「开始生成」。
- 点击开始生成时只做 Prompt 快照并立即进入本地串行提交队列，界面立刻释放输入区，可马上选择下一张图片。
- 后台单线程提交器严格保持用户点击顺序，再由 ComfyUI 负责实际 GPU 队列。
- 本地尚未提交的 Prompt 会持久保存，App 重启后可恢复提交。

### 3. 最近输出后台恢复
- 任务完成时把完整输出 `filename/subfolder/type/timestamp/prompt_id` 持久写入任务记录。
- MainActivity 每次回到前台都会主动恢复「最新完成任务」输出，不再只依赖 Activity 当时是否收到监听事件。
- 切换页面、切到其他 App、熄屏后回来都能补拉最新输出。
- 仍保留 prompt history / 完整 history 作为旧任务和异常输出节点的回退读取路径。

### 4. 图库实时更新
- 进入图库立即刷新，停留期间每 5 秒自动刷新，回到图库也会立即刷新。
- 同时合并 ComfyUI `/history` 与 App 已完成任务的持久输出，避免 history 短暂延迟导致最新图片缺失。
- history 解析改为先收集完整结果、按真实时间排序，再截取最近 100 张；不再在服务器迭代中提前截断。
- 自定义输出节点会尝试深层扫描 filename/subfolder/type。
- 保留时间正/倒序、名称 A-Z/Z-A、长按多选、全选、批量下载。

### 5. 队列页面重做
- 每个任务显示输入图缩略图；完成后优先显示输出缩略图。
- 状态细分：本地等待、提交中、ComfyUI 等待、生成中、同步输出、完成、失败、取消。
- 显示等待队列位置。
- 新增总体进度条与百分比。
- 接入 ComfyUI WebSocket `progress/executing/execution_start` 事件；WebSocket 不可用时自动回退到 history + queue 轮询阶段进度。
- 记录加入时间、Prompt 提交时间、生成开始时间、完成时间。
- 完成任务显示「总用时」与「生成用时」。
- 点击已完成任务或缩略图弹出高清原图。
- 长按完成任务缩略图或预览大图可直接保存。
- 等待任务支持取消，当前运行任务支持 interrupt。

### 6. 数据兼容
- 保留 V1.8 的工作流文件库与 `generation_jobs_v18` 历史键，升级后不主动清空旧工作流/任务记录。
- 保留 V1.8 已验证的 Android 兼容文件 IO 修复：`Files.readAllBytes` / `Files.write`。

## 版本
- versionCode: 9
- versionName: 1.9.0
