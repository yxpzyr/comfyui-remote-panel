# V1.8.0 变更摘要

## 工作流
- 从单工作流改为多工作流常驻库。
- 工作流正文保存到 App 私有文件目录 `workflows_v18`。
- 支持快速切换、重命名、收藏置顶、删除。
- 自动迁移旧版单工作流配置。
- 每个工作流独立保存提示词/开关覆盖值。

## 连续生成队列
- “开始生成”改为“加入队列”。
- 输入图上传并拿到 prompt_id 后立即恢复主界面输入能力，不再等待生成完成。
- 可以连续提交多张输入图，也可以在不同工作流之间交叉提交。
- 新增 QueueActivity：展示上传中/等待中/生成中/同步/完成/失败/取消。
- 等待任务支持从 `/queue` 删除；运行任务支持 `/interrupt`。
- 未完成 prompt_id 会在 App 再次打开时恢复监控。

## history 兼容
- completed 后没有立刻拿到 image metadata 时继续轮询约 30 秒。
- prompt-specific history 无图时回退读取完整 history。
- 支持深层递归寻找 `filename/subfolder/type`。
- 自定义输出节点确实没有可下载图片时，任务记为“完成但无可下载图片”，不再误报生图失败。

## 图库
- 历史图片上限从约 60 提升到 100。
- 增加时间升/降序与名称 A-Z/Z-A 排序。
- 长按进入多选，支持全选/取消选择/批量下载。
- 批量下载显示进度。
- 缩略图和原图保存解耦，图库不长期持有 100 张原始图片 byte[]。

## 版本
- versionCode: 8
- versionName: 1.8.0
