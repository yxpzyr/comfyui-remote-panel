# ComfyUI Remote Panel V2.4.1

## 修正：真正的文件夹式工作流库

V2.4 的“分类筛选”改为文件管理器式的工作流库：

- 点击“工作流库”先进入文件夹根目录，不再先弹分类筛选框。
- 根目录直接提供“＋ 新建文件夹”。
- 根目录显示：★ 常用、未分类、自定义文件夹，并显示各自工作流数量。
- 点击文件夹后进入该文件夹，只显示其中工作流；返回键回到工作流库根目录。
- 工作流支持使用、重命名、移动到其他文件夹、常用、删除。
- 自定义文件夹支持重命名和删除；删除文件夹不会删除工作流，内容自动移回“未分类”。
- 首页横向快捷工作流只显示“当前工作流所在文件夹”的同类工作流，避免不同功能混在一起。

## 新增：导入整个文件夹

- “＋ 导入”现在可选择“导入工作流文件”或“导入整个文件夹”。
- 工作流库根目录也直接提供“📂 导入整个文件夹”。
- 使用 Android 系统目录选择器选择一个手机文件夹后，App 会递归扫描其中 `.json` 工作流。
- 自动使用所选目录名称创建同名 App 工作流文件夹；同名文件夹自动追加 `(2)`、`(3)`。
- 非 JSON 文件自动忽略；单个坏 JSON 只记录失败，不会中断整批导入。
- 文件夹导入逐个解析，显示 `正在导入 N / 总数`，避免一次性将大量工作流加载进内存。
- 单次目录扫描最多 500 个 JSON，防止误选超大目录导致卡死。
- 如果目录内所有 JSON 都导入失败，自动删除这次创建的空 App 文件夹。

## 保留

- V2.4 批量多选 JSON 导入。
- 蒙版双指 1×–10× 缩放、双指平移、单指绘制/擦除。
- V2.3 任务级图库、最终图优先、分页。
- 固定 PKCS12 release 签名与 GitHub Actions `assembleRelease`，可直接覆盖固定签名版 V2.3/V2.4。

## 版本

- versionCode: 15
- versionName: 2.4.1
